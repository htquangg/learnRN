import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 10,
  },
  plusButton: {
    position: 'absolute',
    bottom: 16,
    right: 10,
  },
});

export default styles;
