import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 8,
  },
  scrollTopButton: {
    position: 'absolute',
    bottom: 0,
    right: 10,
  },
});

export default styles;
