import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  image: {
    width: '100%',
    height: 200,
  },
  rightSwipeContainer: {
    flex: 1,
  },
});

export default styles;
